<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalLens">

        <activity
            android:name=".ui.ui.splash.SplashActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".ui.ui.main.MainActivity" />
        <activity android:name=".ui.ui.auth.LoginActivity" />
        <activity android:name=".ui.ui.auth.RegisterActivity" />
        <activity android:name=".ui.ui.home.HomeActivity" />
        <activity android:name=".ui.ui.favorites.FavoritesActivity" />
        <activity android:name=".ui.ui.settings.SettingsActivity" />
        <activity android:name=".ui.ui.profile.ProfileActivity" />
    </application>

</manifest>